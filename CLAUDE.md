# 项目：脑电动力学表征与跨被试适配研究（候选方案评估中）

> 本文件记录项目上下文、证据边界和下一步候选，不把尚未验证的假设当作事实。
> 修订日期：2026-09-09。此次修订未重跑实验，未改变模型计算逻辑。

## 一句话概括

围绕神经动力学与脑电解码，评估 Koopman 约束或谱初始化是否带来独立贡献，
并探索跨被试泛化或少样本适配；情绪识别已有前期实验，运动想象为可选应用。
目前尚未确立两个已验证的创新点，但候选假设可以作为开题的拟开展研究内容。

## 当前证据与决策

- 已完成固定 Hankel/DMD 谱分析和 Deep Koopman Gate 1–3 等前期实验。
- 当前 Deep Koopman 模型是逐帧编码、平均读出；K 通过训练损失影响编码器，
  不参与分类前向计算。已有结果未检出稳定的 Koopman 分类增益。
- 暂停该具体方案的无目标扩展；不把结果外推为所有 Koopman 方法无效。
- RF-SNN 谱初始化和少样本适配尚待实现及验证，均不是已成立的毕业保障。
- SEED 已有数据与预处理基础。是否增加运动想象，应由研究问题和验证成本决定；
  不预设运动想象优先，也不强制同时开展两个任务。

## 数学前提及适用范围

### 1. 线性观测模型是工作假设

设观测为 $x^{(s)}=A_s z$。这里 $z$ 表示潜在状态，$A_s$ 表示观测映射。
“所有被试具有相同潜在动力学、差异仅来自固定线性观测”是需要检验的建模假设。
共享演化规律不意味着不同被试具有相同初值、输入或逐时刻对齐的状态轨迹。

### 2. 伪逆关系不能无条件推出相似

先限于有限维线性状态模型 $z_{t+1}=K_z z_t$，令
$A\in\mathbb R^{m\times n}$、$K_x=A K_z A^+$。

- 若 A 为可逆方阵，则 $K_x=A K_z A^{-1}$ 与 $K_z$ 相似。
- 若 A 满列秩，则 $A^+A=I_n$，$K_x A=A K_z$；A 是源空间到
  $\operatorname{im}(A)$ 的同构，$K_x$ 限制在该像空间上与 $K_z$ 相似。
  若 $m>n$，完整观测空间上的 $K_x$ 还包含 $m-n$ 个额外零特征值。
- 若 A 不满列秩，观测可能丢失模态；还需条件才能保证观测自身具有闭合动力学。
  反例：$K_z=\operatorname{diag}(1,2)$、$A=(1\;0)$，则 $A K_z A^+=(1)$。
  此处 A 是满行秩，不是满列秩。

对一般非线性系统，Koopman 算子作用于观测函数。
从原始电压的线性混合直接得到上述有限维算子关系，还需可观测性、合适的
观测函数空间/不变子空间等条件。有限数据 DMD 估计也不自动等于真实 Koopman 谱。
不得写成“特征值对被试差异严格不变，任何矛盾实现都是错的”。

### 3. 判别性与不变性分开检验

- 情绪可分性弱：当前特征及判据未检出足够的情绪区分信息。
- 被试不变性：需要直接评价被试信息和跨被试变化，不能由情绪可分性推断。
- 谱作为分类特征无明显收益，不直接否定其作为网络初始化或正则项的潜在价值。
- 特征向量携带被试差异不意味着对齐特征空间必然错误；其作用需由实验决定。

## 候选研究内容一：动力学驱动的振荡表征

研究问题：在相同数据、评测协议和训练预算下，Koopman 相关设计能否改善
准确率、泛化、收敛速度或计算成本中的预先指定指标？各指标分别报告，不混用。

一个候选是用训练数据估计的频率与增长/衰减率初始化 RF/BRF 神经元。
这是待验证的归纳偏置，不能从“EEG 含振荡”直接推出有效性或新颖性。

实现前明确：

1. 使用哪些模态、如何加权；“谱峰值”采用什么定义。
2. 离散特征值到连续参数的映射：
   $\sigma=\log|\lambda|/\Delta t$、$f=\arg(\lambda)/(2\pi\Delta t)$；
   记录分支、混叠范围和实际时间间隔。不能混淆原始采样间隔与帧间隔。
3. 到 RF 参数的映射须依照实际神经元方程与数值离散化；处理稳定性、正增长模态
   及参数范围，不把单神经元参数直接称为整个脉冲网络的 Koopman 谱。
4. 初始化仅从该折允许使用的数据估计。使用目标被试数据须明确为适配设置。

建议对照：RF 随机初始化、RF 均匀频率初始化、RF 普通功率谱初始化、
RF Koopman 初始化。DE 若作为额外对照，必须先定义具体的特征到参数映射。
与 LIF/ALIF 的比较回答神经元类型的作用；RF 内部的初始化比较才回答初始化贡献。
现有相关工作是否已采用相似方法，需查阅原文后确认，不预写“现有方法都只用随机初始化”。

## 候选研究内容二：跨被试泛化或少样本适配

研究问题：怎样降低未知被试上的性能下降，或在固定标定预算下提高适配效果？
应先选定主要设置，分别报告：

- 零样本泛化：训练和选择模型时不使用目标被试数据。
- 少样本监督适配：只使用指定的 K 个目标被试标注 trial 及其允许信息。
- 若还使用目标被试未标注数据，另列为半监督/无监督适配设置。

最小二乘 $\hat A_s=X_s Z^+$ 仅在配对的 Z 已知、维度和秩条件适当时才成立。
必须说明 Z 如何获得，以及如何与新被试 X 配对；不能直接借用其他被试的轨迹。
目前没有依据承诺“理论上 5–10 个 trial 即可完成标定”。

待设计项：K 的含义（总数或每类数）、标定 trial 的抽样规则、重复抽样、
更新的参数、超参数选择方式、剩余 trial 的独立测试，以及同预算下的线性读出适配、
普通微调等对照。不得把零样本 LOSO 本身当成少样本适配实验。

两个候选研究内容应分别验证，适配模块可先依托可靠基线，
不强制依赖谱初始化成功。DE 可以作为特征基线，但不能把电压线性混合定理
直接搬到功率/DE 空间：混合后方差含协方差项，DE 的对数变换也不保持原线性关系。

## 已完成验证与来源边界

以下数字沿用既有项目记录；本次只修订解释，没有重新计算。

| 项目 | 已记录结果 | 允许的解释 |
|---|---|---|
| 合成自治系统 DMD 频率 | 显示误差 0.000 Hz | 该测试条件和显示精度下结果正确，不代表所有情况都正确 |
| 合成谱分离比 | 4.0 | 仅适用于对应合成配置；具体统计定义以原脚本为准 |
| 合成低信噪比测试 | SNR 20→−10 dB，谱误差 0.16–0.23 | 不直接推广到真实 EEG |
| 初始超参数 | d=16、r=16 | 历史起点，不是理论保证 |
| 合成随机驱动测试 | 频率误差约 0.5–1 Hz；增加时延后约 0.05–0.26 Hz | 不保证真实 EEG 同样改善 |
| SEED 固定谱情绪可分性 | 1.002 / 1.002 / 1.001，未显著；旧摘要记 p≈0.4 | 当前固定表征下未检出判别性；p 值须对应具体 session 与检验，不合并为一个总 p |
| Deep Koopman LOSO | A=0.591、B=0.588、C=0.574、D1=0.585、DE=0.527 | C−B p=0.292；C−DE p=0.312（来自先前完整日志），均未显著 |
| RF-SNN 谱初始化 | 尚未验证 | 无支持或否定其效果的直接结果 |

合成代码：`src/koopman_stage0_synth.py`；Deep Koopman 报告：
`results/koopman_gate/report_gates.md`。不同实验的 session、划分、输入和基线不能混比。

## 可选的有限补充验证

若训练流程检查未发现具体问题，且仍需回答读出方式的限制，可预先限定一组实验：
原平均读出与同一个小型时序读出，各自比较 B、C、D1，共六组。
新读出下重点比较 C−B 和 C−D1；各组同时提升仅支持时序读出，不能归功于 Koopman。
保持数据划分、调参预算和评测流程一致。是否执行、预算及最低有用效应先记录。
已反复查看的评测数据上的结果标为探索性，后续保留独立确认。
不因一次非显著就否定全部方法，也不无限更换设置寻找显著结果。

## 技术约束与已知风险

1. 标准 DMD 是所选观测下的线性演化估计；随机驱动、非平稳性与观测噪声会影响结果。
   时延嵌入能否改善需在实际数据上验证，不把合成实验的偏差或 d≥16 写成普遍规律。
2. $|\lambda|>1$ 可能导致长时外推增长，但在频率/阻尼距离中不一定产生指数放大。
   例如 $1.05^{800}$ 约为 $10^{17}$，这个例子只解释反复传播的风险。
   截断秩、fbDMD、频率过滤、mod_max、top_k、幅度加权和单位圆投影须分别记录，
   不能默认全部必需；投影会改变阻尼信息。超参数只按允许的训练/验证数据选择。
3. 统一比较谱时记录采样率和步长，优先使用有明确单位的 $(f,\sigma)$。
4. LOSO 标准化、数据驱动通道筛选和谱初始化均须核对折内拟合范围。
   先划分 trial/session/被试，再分段，避免同 trial 的相关片段跨训练与测试。
5. 核对损失权重、目标帧配对、梯度路径、BatchNorm 训练/评估模式、
   C_cf 重估所用数据、重估频率与 checkpoint 保存/恢复；模型定义本身不足以验证这些步骤。

## 数据集与本地数据接口

| 数据集 | 当前角色 | 格式记录或待确认项 |
|---|---|---|
| SEED | 已有主要实验基础 | 本地 `Preprocessed_EEG/`，MAT5，变量 `缩写_eegN`，(62,T)，200 Hz，标签 {-1,0,1} |
| BCI Competition IV-2a | 可选运动想象验证 | 官方 9 被试、22 EEG＋3 EOG、250 Hz、4 类、每被试 2 session，每 session 288 trial；尚未确认本地下载 |
| SEED-IV | 可选后续验证 | 文件结构和各 session 标签以实际发行说明为准 |
| DEAP | 可选后续验证 | 既有记录为预处理 pickle `.dat`，(40,40,8064)，前 32 通道 EEG、前 3 秒基线、128 Hz；使用前确认版本 |
| SEED-V / VII | 后期可选 | 不自动加入当前开题主计划 |

统一接口：`load_trials(dataset, subject, session) -> (X, y, fs)`。
当前原始 EEG DMD 流程需要时间序列；DE 留作对照。对 DE 时间序列另建动力学模型
属于另一种观测函数设计，不能与当前原始信号流程混为一谈。

本地 SEED 历史核验（2026-08-10，`E:\Python\DATA\SEED`；本次未复查）：

- 45 个被试文件（15×3 session）及 `label.mat`、`readme.txt`，约 6.96 GB。
- `label.mat` 为 (1,15)，每文件 `_eeg1`–`_eeg15`；原记录为 62 通道，
  无 NaN/Inf/恒定通道，各 trial 185–265 s，每文件合计 678,815 点。
- 原诊断记录 75 Hz 前后跌幅 2.41 dB、50 Hz 处 −9.24 dB、
  奈奎斯特附近 +3.67 dB。仅凭这些功率谱数字不能断言官方滤波不存在或工频已完全消除；
  论文应区分发行说明、自行处理与本地观测。
- 幅值记录 median |x|≈10、max≈1.4e4–3.2e4。大瞬变可能是伪迹，
  不能仅凭幅值确认来源；剔除规则需记录阈值、拟合范围、剔除比例及类别分布。
- `6_20130712.mat` 约 308 MB，其他文件约 155 MB；历史核验归因为未压缩存储，
  采样点数相同。

读取维度按实际文件确认：本地 MAT5 的 scipy 分支已记录为 (62,T)；
HDF5/h5py 分支若确认为 (T,62) 再转置。不得对所有版本无条件转置。

## 工程规范与历史环境

- 缓存按预处理、分段、估谱、距离计算分层；缓存键包含影响结果的参数和代码版本。
  涉及训练拟合的缓存还应区分折与训练集合。
- 长任务支持断点续跑；保存配置、随机种子、划分、运行日志和逐被试结果。
- 保留已经验证的 NumPy/SciPy 实现；不为满足工具名称而迁移依赖。
  POT、MNE、PyDMD、SpikingJelly、snntorch、W&B 按实际需求选用。
- 原项目目录：`E:\Python\Study\Koopman_EEG`。
  历史运行解释器：`D:\Anaconda\envs\pytorch2.2.2\python.exe`。
- 旧环境记录：Python 3.9.21、torch 2.2.2、snntorch 0.9.4、numpy 1.26.4、
  scipy 1.13.1、matplotlib 3.9.2、sklearn 1.6.1、h5py 3.13.0，及 pandas/joblib 等。
  2026-09-09 对话中环境输出显示 POT 0.9.7.post1 可导入，
  mne/pydmd/spikingjelly/wandb 缺失。执行前用项目解释器核对，勿用 base 环境推断。

## 评价与能效

报告准确率、Macro-F1、逐被试结果、配对效应及不确定性；
各指标的汇总单位和模型选择依据预先明确。少样本实验另报标定预算和重复抽样结果。

SNN 报告时间步 T、逐层平均发放率、参数量、突触操作估计及测量平台。
$S=T\bar r$ 仅是活动摘要；不能仅凭 $S<5$ 宣称能效优于 ANN。
能耗估计须说明逐层 MAC/AC 数量、单位操作成本、神经元状态更新与内存开销；
模型估算与实测功耗分开。BRF 振荡状态更新本身也可能有额外成本。

## 工作约定

- 不承诺某周必须获得显著提升，时间表以实验与文档的可完成产物为标准。
- 不把无显著差异称为证明无效；暂停投入是研究决策，需与统计结论分开。
- 修改假设或协议时说明依据；保留历史结果、日期和修订说明。
- 新方向先查原始文献、定义对照并做有限预实验；不把方法组合自动称为创新。
- 新颖性、学校开题要求及临床康复价值分别核实，不把公开数据分类效果当成康复疗效。

## 参考依据

- Brunton et al., 2016, *Koopman Invariant Subspaces and Finite Linear Representations of Nonlinear Dynamical Systems for Control*.
  https://doi.org/10.1371/journal.pone.0150171
- Higuchi et al., 2024, *Balanced Resonate-and-Fire Neurons*.
  https://proceedings.mlr.press/v235/higuchi24a.html
- BCI Competition IV-2a 官方数据说明：
  https://www.bbci.de/competition/iv/desc_2a.pdf
